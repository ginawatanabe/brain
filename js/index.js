var lhcaudalanteriorcingulate = {
  name: 'Caudal Anterior Cingulate'
}

var lhcaudalmiddlefrontal = {
  name: 'Caudal Middle Frontal'
}

var lhcuneus = {
  name: 'Cuneus'
}

var lhentorhinal = {
  name: 'Entorhinal'
}

var lhfusiform = {
  name: 'Fusiform',
  keywords: 'face',
  specific: 'perception',
  source: 'http://www.jneurosci.org/content/34/38/12828'
}

var lhinferiorparietal = {
  name: 'Inferior Parietal Lobule',
  keywords: 'attention language action',
  specific: 'processing'
}

var lhinferiortemporal = {
  name: 'Inferior Temporal Lobule'
}

var lhinsula = {
  name: 'Insula'
}

var lhisthmuscingulate = {
  name: 'Isthmus Cingulate'
}

var lhlateraloccipital = {
  name: 'Lateral Occipital Complex'
}

var lhlateralorbitofrontal = {
  name: 'Lateral Orbito Frontal Cortex'
}

var lhlingual = {
  name: 'Lingual Gyrus'
}

var lhmedialorbitofrontal = {
  name: 'Medial Orbito Frontal Cortex'
}

var lhmiddletemporal = {
  name: 'Middle Temporal Gyrus'
}

var lhparacentral = {
  name: 'Paracentral Lobule'
}

var lhparahippocampal = {
  name: 'Parahippocampal Gyrus'
}

var lhparsopercularis = {
  name: 'Pars Opercularis'
}

var lhparsorbitalis = {
  name: 'Pars Orbitalis'
}

var lhparstriangularis = {
  name: 'Pars Triangularis'
}

var lhpericalcarine = {
  name: 'Pericalcarine'
}

var lhpostcentral = {
  name: 'Post Central Gyrus'
}

var lhposteriorcingulate = {
  name: 'Posterior Cingulate'
}

var lhprecentral = {
  name: 'Precentral Gyrus'
}

var lhprecuneus = {
  name: 'Precuneus'
}

var lhrostralanteriorcingulate = {
  name: 'Rostral Anterior Cingulate'
}

var lhrostralmiddlefrontal = {
  name: 'Rostral Middle Frontal Gyrus'
}

var lhsuperiorfrontal = {
  name: 'Superior Frontal Gyrus'
}

var lhsuperiorparietal = {
  name: 'Superior Parietal Gyrus'
}

var lhsuperiortemporal = {
  name: 'Superior Temporal Gyrus'
}

var lhsupramarginal = {
  name: 'Supramarginal Gyrus'
}

var lhtransversetemporal = {
  name: 'Transverse Temporal Gyrus'
}

var lhunknown = {
  name: 'Unknown'
}

var rhcaudalanteriorcingulate = {
  name: 'Caudal Anterior Cingulate'
}

var rhcaudalmiddlefrontal = {
  name: 'Caudal Middle Frontal'
}

var rhcuneus = {
  name: 'Cuneus'
}

var rhentorhinal = {
  name: 'Entorhinal'
}

var rhfusiform = {
  name: 'Fusiform'
}

var rhinferiorparietal = {
  name: 'Inferior Parietal Lobule'
}

var rhinferiortemporal = {
  name: 'Inferior Temporal Lobule'
}

var rhinsula = {
  name: 'Insula'
}

var rhisthmuscingulate = {
  name: 'Isthmus Cingulate'
}

var rhlateraloccipital = {
  name: 'Lateral Occipital Complex'
}

var rhlateralorbitofrontal = {
  name: 'Lateral Orbito Frontal Cortex'
}

var rhlingual = {
  name: 'Lingual Gyrus'
}

var rhmedialorbitofrontal = {
  name: 'Medial Orbito Frontal Cortex'
}

var rhmiddletemporal = {
  name: 'Middle Temporal Gyrus'
}

var rhparacentral = {
  name: 'Paracentral Lobule'
}

var rhparahippocampal = {
  name: 'Parahippocampal Gyrus'
}

var rhparsopercularis = {
  name: 'Pars Opercularis'
}

var rhparsorbitalis = {
  name: 'Pars Orbitalis'
}

var rhparstriangularis = {
  name: 'Pars Triangularis'
}

var rhpericalcarine = {
  name: 'Pericalcarine'
}

var rhpostcentral = {
  name: 'Post Central Gyrus'
}

var rhposteriorcingulate = {
  name: 'Posterior Cingulate'
}

var rhprecentral = {
  name: 'Precentral Gyrus'
}

var rhprecuneus = {
  name: 'Precuneus'
}

var rhrostralanteriorcingulate = {
  name: 'Rostral Anterior Cingulate'
}

var rhrostralmiddlefrontal = {
  name: 'Rostral Middle Frontal Gyrus'
}

var rhsuperiorfrontal = {
  name: 'Superior Frontal Gyrus'
}

var rhsuperiorparietal = {
  name: 'Superior Parietal Gyrus'
}

var rhsuperiortemporal = {
  name: 'Superior Temporal Gyrus'
}

var rhsupramarginal = {
  name: 'Supramarginal Gyrus'
}

var rhtransversetemporal = {
  name: 'Transverse Temporal Gyrus'
}

var rhunknown = {
  name: 'Unknown'
}

var parts =
[lhcaudalanteriorcingulate, lhcaudalmiddlefrontal, lhcuneus, lhentorhinal,lhfusiform,
lhinferiorparietal, lhinferiortemporal, lhinsula, lhisthmuscingulate, lhlateraloccipital,
lhlateralorbitofrontal, lhlingual, lhmedialorbitofrontal, lhmiddletemporal, lhparacentral,
lhparahippocampal, lhparsopercularis, lhparsorbitalis, lhparstriangularis, lhpericalcarine,
lhpostcentral, lhposteriorcingulate, lhprecentral, lhprecuneus, lhrostralanteriorcingulate,
lhrostralmiddlefrontal, lhsuperiorfrontal, lhsuperiorparietal, lhsuperiortemporal, lhsupramarginal,
lhtransversetemporal, lhunknown, rhcaudalanteriorcingulate, rhcaudalmiddlefrontal, rhcuneus,
rhentorhinal, rhfusiform, rhinferiorparietal, rhinferiortemporal, rhinsula, rhisthmuscingulate,
rhlateraloccipital, rhlateralorbitofrontal, rhlingual, rhmedialorbitofrontal, rhmiddletemporal,
rhparacentral, rhparahippocampal, rhparsopercularis, rhparsorbitalis, rhparstriangularis, rhpericalcarine, rhpostcentral,
rhposteriorcingulate, rhprecentral, rhprecuneus, rhrostralanteriorcingulate, rhrostralmiddlefrontal,
rhsuperiorfrontal, rhsuperiorparietal, rhsuperiortemporal, rhsupramarginal, rhtransversetemporal, rhunknown];

var fact1 = {
  fact: "The brain is 73% water.",
  source: "https://water.usgs.gov/edu/propertyyou.html"
}

var fact2 = {
  fact: "We have anywhere from 25,000 to 50,000 thoughts a day.",
  source: "https://www.psychologytoday.com/articles/200107/depression-doing-the-thinking"
}

var fact3 = {
  fact: "The average attention span in 2015 was 8 seconds.",
  source: "http://www.statisticbrain.com/attention-span-statistics/"
}

var fact4 = {
  fact: "The brain's memory storage capacity is close to around 2.5 petabytes.",
  source: "https://www.scientificamerican.com/article/what-is-the-memory-capacity/"
}

var fact5 = {
  fact: "By the age of 2, the brain is 80% of adult size.",
  source: "https://faculty.washington.edu/chudler/dev.html"
}

var fact6 = {
  fact: "The brain makes up about 2% of a human's body weight.",
  source: "https://www.livescience.com/29365-human-brain.html"
}

var fact7 = {
  fact: "The brain contains about 86 billion neurons.",
  source: "https://www.livescience.com/29365-human-brain.html"
}

var fact8 = {
  fact: "The brain weighs about 3.3 lbs (1.5 kg).",
  source: "https://www.livescience.com/29365-human-brain.html"
}

var fact9 = {
  fact: "The brain uses 20% of oxygen breathed.",
  source: "https://www.brainhq.com/brain-resources/brain-facts-myths/brain-facts"
}

var fact10 = {
  fact: "Approximately 20% of the blood from the heart is pumped to the brain.",
  source: "https://www.brainhq.com/brain-resources/brain-facts-myths/brain-facts"
}

var brainfacts = [fact1, fact2, fact3, fact4, fact5, fact6, fact7, fact8, fact9, fact10];

window.onload = function() {
  var loadTime = window.performance.timing.domContentLoadedEventEnd-window.performance.timing.navigationStart;
  console.log('Page load time is '+ loadTime);

  let name = document.getElementById('name');
  var brainsource = document.getElementById('brainfact-source');

  // let detail = document.getElementById('detail');
  // let source = document.getElementById('source');

  let container, scene, camera, renderer, light, controls;
  let lightIntensity = 1.4;

  let targetRotationX = 0;
  let targetRotationOnMouseDownX = 0;

  let targetRotationY = 0;
  let targetRotationOnMouseDownY = 0;

  let mouseX = 0;
  let mouseXOnMouseDown = 0;

  let mouseY = 0;
  let mouseYOnMouseDown = 0;

  let windowHalfX = window.innerWidth/2;
  let windowHalfY = window.innerHeight/2;

  let WIDTH  = window.innerWidth;
  let HEIGHT = window.innerHeight;
  let SPEED = 0.01;

  let raycaster = new THREE.Raycaster();
  let mouse = new THREE.Vector2(), INTERSECTED, CLICKINTERSECTED;
  let intersects;

  let loading = setTimeout(go, loadTime+5500);

  var currentMeshName;

  // Setting up GUI.
  let params = {
    lightIntensity: 1.4
  }
  let gui = new dat.GUI();
  gui.add(params, 'lightIntensity', 0, 2).step(0.01).onChange(function(value) {
    light.intensity = value;
  });
  gui.open();

  let group = new THREE.Group();

  let mesh = null;

  init();
  animate();

  //Brainfact
  (function showFact() {
    let brainfact = document.getElementById('brainfact');
    let random = Math.floor(Math.random()*brainfacts.length);
    console.log(random);
    brainfact.innerHTML = brainfacts[random].fact;
    brainsource.href = brainfacts[random].source;
  })();

  //Loading Screen

  function go() {
    let overlay = document.getElementById('overlay-wrap');
    overlay.style.display = "none";
    //Tween entrance animation.
    let position = {x : -40, y : -40, z : -600, rx : -0.5, ry : -2.5};
    let target = { x : -50, y: -50, z : -200, rx : 0, ry : 0};
    let tween = new TWEEN.Tween(position).to(target, 3700);
    tween.easing(TWEEN.Easing.Exponential.Out);

    tween.start();
    tween.onUpdate(function(){
      group.position.x = position.x;
      group.position.y = position.y;
      group.position.z = position.z;
      group.rotation.x = position.rx;
      group.rotation.y = position.ry;
    })
  }

  function init() {


    container = document.createElement('div');
    document.body.appendChild(container);

    scene = new THREE.Scene();
    scene.add(group);

    // initCamera();
    camera = new THREE.PerspectiveCamera(70, WIDTH / HEIGHT, 1, 500);
    camera.position.set(0, 3.5, 10);
    camera.lookAt(scene.position);

    // initMesh();

    for (let i=0; i<64; i++) {
      let loader = new THREE.JSONLoader();
      let name = 'models/brain/part' + String(i) + '.json';
      let meshName = parts[i].name;
      loader.load(name, function(geometry, materials) {
        mesh = new THREE.Mesh(geometry, new THREE.MeshFaceMaterial(materials));
        mesh.rotation.y= -10;
        mesh.name = meshName;
        group.add(mesh);
      });
    };

    // initLight();
    light = new THREE.PointLight(0xffffff, lightIntensity);
    light.position.set(50,50,50);
    // light.castShadow = false;
    scene.add(light);
    console.log(scene.children);

    // initRenderer();
    renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
    renderer.setPixelRatio(window.devicePixelRatio);
    renderer.setSize(WIDTH, HEIGHT);
    renderer.sortObjects = false;

    container.appendChild(renderer.domElement);
  }


  function onResize() {
    windowHalfX = window.innerWidth/2;
    windowHalfY = window.innerHeight/2;
    camera.aspect = window.innerWidth/window.innerHeight;
    camera.updateProjectionMatrix();
    renderer.setSize(window.innerWidth, window.innerHeight);
  }

  function onMouseDown() {
    event.preventDefault();
    document.addEventListener('mousemove', onMouseMove, false);
    document.addEventListener('mouseup', onMouseUp);
    document.addEventListener('mouseout', onMouseOut, false);

    mouseXOnMouseDown = event.clientX - windowHalfX;
    targetRotationOnMouseDownX = targetRotationX;

    mouseYOnMouseDown = event.clientY - windowHalfY;
    targetRotationOnMouseDownY = targetRotationY;

  }

  function onMouseUp(event) {
    document.removeEventListener('mousemove', onMouseMove, false);
    document.removeEventListener('mouseup', onMouseUp, false);
    document.removeEventListener('mouseout', onMouseOut, false);
  }

  function onMouseOut(event) {
    document.removeEventListener('mousemove', onMouseMove, false);
    document.removeEventListener('mouseup', onMouseUp, false);
    document.removeEventListener('mouseout', onMouseOut, false);
  }

  function onTouchStart(event) {
    if (event.touches.length == 1) {
      event.preventDefault();
      mouseXOnMouseDown = event.touches[0].pageX - windowHalfX;
      targetRotationOnMouseDownX = targetRotationX;
      mouseYOnMouseDown = event.touches[0].pageY - windowHalfY;
      targetRotationOnMouseDownY = targetRotationY;
    }
  }

  function onTouchMove(event) {
    if (event.touches.length == 1) {
      console.log('hello');
      event.preventDefault();
      mouseX = event.touches[0].pageX - windowHalfX;
      targetRotationX = targetRotationOnMouseDownX + (mouseX - mouseXOnMouseDown) * 0.05;
      mouseY = event.touches[0].pageY - windowHalfY;
      targetRotationY = targetRotationOnMouseDownY + (mouseY = mouseYOnMouseDown) * 0.05;
    }
  }

  function onMouseMove(event) {
    console.log('mouse is moving');
    mouseX = event.clientX - windowHalfX;
    mouseY = event.clientY - windowHalfY;
    targetRotationX = targetRotationOnMouseDownX + ( mouseX - mouseXOnMouseDown) * 0.02;
    targetRotationY = targetRotationOnMouseDownY + ( mouseY - mouseYOnMouseDown) * 0.02;
  }

  let count = 0;

  function onDocumentMouseMove(event) {
    mouse.x = (event.clientX/window.innerWidth)*2 - 1;
    mouse.y = -(event.clientY/window.innerHeight)*2 + 1;
    // Raycaster
    // Update the picking ray with the camera and mouse position.
    raycaster.setFromCamera(mouse, camera);
    // Calculate objects intersecting the picking ray
    intersects = raycaster.intersectObjects(scene.children[0].children);

    if (intersects.length > 0) {

      if (intersects[0].object != INTERSECTED) {
        if (INTERSECTED) {
          INTERSECTED.material.materials[0].emissive.setRGB(0,0,0);
          let description = document.getElementById('description');
          description.innerHTML = "";
          count=0;
        }
      }

      INTERSECTED = intersects[0].object;
      name.innerHTML = intersects[0].object.name.toUpperCase();
      currentMeshName = intersects[0].object.name;

      intersects[0].object.material.materials[0].emissive.setRGB(0.3,0.3,0.3);
      count++;
    }
  }

  // function onClick() {
  //   console.log(currentMeshName);
  //
  //     $.getJSON("https://en.wikipedia.org/w/api.php?action=query&format=json&gsrlimit=3&generator=search&origin=*&gsrsearch=" + currentMeshName, function(data){
  //         $.each(data.query.pages, function (i) {
  //          console.log(data.query.pages[i]);
  //         $('#description').append("<p><a href='https://en.wikipedia.org/?curid=" + data.query.pages[i].pageid +"' target='_blank'>" + data.query.pages[i].title + "</a></p>");
  //
  //         });
  //       });
  //
  // }

  function render() {
    group.rotation.y += (targetRotationX - group.rotation.y) * 0.05;
    group.rotation.x += (targetRotationY - group.rotation.x) * 0.05;
    renderer.render(scene,camera);
    TWEEN.update();
  }

  function animate() {
    requestAnimationFrame(animate);
    render();
  }



  window.addEventListener('resize',onResize,false);

  window.addEventListener('mousedown', onMouseDown, false);

  window.addEventListener('touchmove', onTouchMove, false);

  window.addEventListener('touchstart',onTouchStart,false);

  window.addEventListener('mousemove', onDocumentMouseMove, false);

  window.addEventListener('click', onClick, false);

}
